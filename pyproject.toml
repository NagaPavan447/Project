[project]
name = "project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "NagaPavan447", email = "nagapavan.447@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.16",
]

[project.scripts]
project = "project:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
